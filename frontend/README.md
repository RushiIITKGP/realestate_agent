# HomeGuide AI — Frontend

Minimal Vite chat UI that streams responses from `POST /chat/stream`.

## Run

1. Start the backend on port 8000 (see `backend/README.md`)
2. Then:

```bash
npm install
npm run dev
```

Open http://localhost:5173
