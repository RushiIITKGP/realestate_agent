services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/realestate.db
      CHECKPOINT_DB_URL: sqlite:////app/data/checkpoints.db
    volumes:
      - ./data:/app/data
